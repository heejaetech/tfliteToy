<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.tensorflow.lite.examples.posenettest">

  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.INTERNET" />

  <uses-feature android:name="android.hardware.camera" />
  <uses-feature android:name="android.hardware.camera.autofocus" />

  <uses-sdk />

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/tfe_pn_app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme.Posenet"
      android:usesCleartextTraffic="true">
    <activity
        android:name="org.tensorflow.lite.examples.posenettest.TestActivity"
        android:label="@string/tfe_pn_app_name"
        android:exported="true">
    </activity>
    <activity
        android:name="org.tensorflow.lite.examples.posenettest.CameraActivity"
        android:label="@string/tfe_pn_app_name"
        android:exported="true">
    </activity>

    <!-- 시작 액티비티 -->
<!--    <activity android:name=".CameraActivity" android:exported="true" android:configChanges="orientation|screenSize|keyboard|keyboardHidden">-->
    <activity android:name="org.tensorflow.lite.examples.posenettest.MainActivity" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
<!--        <action android:name="android.intent.action.VIEW"/>-->
<!--        <category android:name="android.intent.category.DEFAULT"/>-->
<!--          <category android:name="android.intent.category.BROWSABLE"/>-->
<!--          <data android:scheme="https" android:host="wedatalab.com"/>-->
      </intent-filter>
    </activity>

  </application>

</manifest>
